# Simple React Context API Lesson

This project teaches a very simple Context API idea:

1. `DataContext.jsx` gets users from JSONPlaceholder.
2. `Navbar.jsx`, `MainSection.jsx`, and `Footer.jsx` all use that same data.
3. Tailwind CSS styles the page.

## Run

```bash
npm install
npm run dev
```

